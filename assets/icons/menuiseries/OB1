<!-- WorkIt - OB1 (Oscillo-battant 1 vantail) | base OF1 + badge "OB" | currentColor -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" fill="none">
  <!-- Traits (structure) -->
  <g stroke="currentColor" stroke-width="22" stroke-linecap="round" stroke-linejoin="round">
    <!-- Cadre extérieur -->
    <rect x="96" y="80" width="320" height="352" rx="24"/>

    <!-- Ouvrant (cadre intérieur) -->
    <rect x="124" y="108" width="264" height="296" rx="18"/>

    <!-- Poignée (côté droit) -->
    <line x1="364" y1="240" x2="364" y2="292"/>

    <!-- Paumelles (côté gauche) -->
    <line x1="146" y1="140" x2="146" y2="170"/>
    <line x1="146" y1="342" x2="146" y2="372"/>
  </g>

  <!-- Badge "OB" (indication métier) -->
  <g stroke="currentColor" stroke-width="18" stroke-linecap="round" stroke-linejoin="round">
    <!-- pastille -->
    <rect x="154" y="124" width="92" height="56" rx="16"/>
  </g>

  <!-- Texte "OB" (en fill pour rester lisible) -->
  <text x="200" y="162"
        text-anchor="middle"
        font-family="system-ui, -apple-system, Segoe UI, Roboto, Arial"
        font-size="28"
        font-weight="700"
        fill="currentColor">OB</text>
</svg>
